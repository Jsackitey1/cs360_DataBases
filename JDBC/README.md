# JDBC Database Connection Test

A simple Java application designed to demonstrate JDBC (Java Database Connectivity) to a MySQL database. It contains examples of how to connect to the database, query data, and structure queries in Java.

## Overview
- **Language:** Java
- **Database:** MySQL
- Utilizes `mysql-connector-java` for connecting to the `sackjo02` database.
